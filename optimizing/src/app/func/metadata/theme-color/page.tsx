import { Metadata } from "next";

// @deprecated nextjs14
/*export const metadata: Metadata = {
    themeColor: 'black',
};*/

export default function Page() {
    return (
        <div>thmecolor</div>
    );
}