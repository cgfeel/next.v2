import { ThemeConfig } from "antd";

const theme: ThemeConfig = {
    token: {
        colorPrimary: '#52c41a',
        colorText: '#fff',
        fontSize: 16,
        colorPrimaryBg: '#164c7e',
    }
};

export default theme;